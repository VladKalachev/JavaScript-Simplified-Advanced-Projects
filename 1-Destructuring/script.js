const array = ["A", "B", "C", "D"]
const person = {
    name: "Kyle",
    age: 25,
    fovoriteFood: "Rice",
    address: {
        street: "123 Main St",
        ciry: "Somewhere",
    }
}

const first = array[0]
const second = array[1]

console.log(first);
console.log(second);